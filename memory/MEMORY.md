# Agent Memory

Initialized per RFC 0009.

Operational memory lives here. See `patterns/` for validated per-family patterns, `dead-ends.jsonl` for anti-patterns, and `recent.log` for per-task one-line reflections.
